- @content_for_title = 'Resend unlock instructions'

= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post, :class => 'mainForm' }) do |f|
  %fieldset
    = devise_error_messages!
    .loginRow.noborder
      = f.label :email
      .loginInput
        = f.text_field :email
      .fix
    .loginRow
      = f.submit 'Resend unlock instructions', :class => 'greenBtn submitForm'
      .fix
= render :partial => "devise/shared/links"