%h1 Listing settings

%table
  %tr
    %th User
    %th Lectionary order
    %th Hymn count
    %th Hymnal
    %th Bible translation
    %th
    %th
    %th

  - @settings.each do |setting|
    %tr
      %td= setting.user_id
      %td= setting.lectionary_order_id
      %td= setting.hymn_count
      %td= setting.hymnal_id
      %td= setting.bible_translation_id
      %td= link_to 'Show', setting
      %td= link_to 'Edit', edit_setting_path(setting)
      %td= link_to 'Destroy', setting, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Setting', new_setting_path
